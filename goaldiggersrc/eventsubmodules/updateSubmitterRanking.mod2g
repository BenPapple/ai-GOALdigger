use "../goaldiggerProlog" as knowledge.
use "../goaldiggerAction" as actionspec.


module updateSubmitterRanking {
	
	if percept(step(3), teamSize(SizeTeam)), bel(positionInHirarchie(PositionInHirarchie), PosCheck is SizeTeam - PositionInHirarchie, PosCheck == 1)
		then insert(submitterLeader).
		
	% ToDo Status
	%forall (_).sent(otherAgentStatus(OtherName, X, Y, Step, Seed, BlockTypeAttached))) do . 
}